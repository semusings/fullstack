spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
        format_sql: true
openfga:
  api-url: http://localhost:4000
  store-id: order
  authorization-model-id: order